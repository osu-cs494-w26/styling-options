# Options for Styling a React App

This repo contains several subprojects, each of which we'll use to explore a different option for styling a React app.  Each subproject is a separate NPM project with its own dependencies, etc.   Please navigate into one of the individual subproject directories to see more.
